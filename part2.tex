\section{Архитектура и проектирование модулей системы}

\subsection{Общая архитектура системы и сценарий работы}

Разрабатываемая интеллектуальная обучающая система (ИОС) реализована как веб-приложение на 
базе фреймворка Django с использованием архитектурного паттерна Model-View-Template (MVT). 
Архитектура системы подразделяется на шесть функциональных модулей: управление пользователями 
и ролевой доступ, конструктор учебного контента, модуль психодиагностики и когнитивного 
профилирования, адаптивная система тестирования на базе IRT, модуль контекстной адаптации 
и подсистема аналитики с рекомендательным движком. Взаимодействие между модулями построено 
по принципу слабой связанности (Loose Coupling) с активным использованием сигнального механизма Django и RESTful API-эндпоинтов, что обеспечивает независимое масштабирование компонентов и упрощает их тестирование.

Полный сценарий взаимодействия системы с пользователем охватывает непрерывный цикл обучения 
и разворачивается в несколько последовательных этапов. На этапе регистрации и инициализации 
студент проходит регистрацию через стандартную форму, после чего система создаёт учётную запись 
со статусом неактивной до подтверждения администратором, инициализирует нулевой счётчик баллов 
активности и запускает фоновый сбор поведенческих данных. При первом входе выполняется запрос к 
внешнему API для определения геолокации по IP-адресу и инициализируется сессия трекинга активности мыши.

Следующим этапом является психологическое тестирование и профилирование, в ходе которого студент 
проходит серию психометрических тестов. Система обрабатывает сырые баллы, рассчитывает ключевые 
факторы A1--A4 и формирует многомерный когнитивный профиль, содержащий персонализированные 
рекомендации в формате T/R/C (для преподавателя, обратная связь, для комьюнити). На основе 
этих данных рекомендательный движок синтезирует информацию о когнитивном профиле, поведенческие 
метрики, рассчитанные на основе трекинга мыши, и текущее значение параметра контекстной 
активности $P$, после чего формирует индивидуальную траекторию и ранжированный список курсов.

В процессе обучения с контрольными точками студент осваивает материал через интерфейс просмотра 
тем, где подача контента может адаптироваться под предпочтительный формат. В контрольных точках 
система инициирует адаптивное тестирование, обновляет оценку способности по модели 
Раша, фиксирует прогресс и пересчитывает контекстный параметр $P$. На основе обновлённых данных 
система корректирует сложность последующих заданий, а модуль мотивационной поддержки начисляет 
баллы за активность и успешное прохождение тем, причём визуализация прогресса и соревновательные 
элементы динамически скрываются или усиливаются в зависимости от уровня нейротизма (A3) студента.

Отдельное внимание уделяется визуализации психологических профилей студентов. Для каждого 
обучающегося, у которого сформирован агрегированный профиль, система предоставляет 
детализированную страницу с разбивкой по факторам A1--A4, где каждое значение сопровождается 
текстовой интерпретацией (например, «выраженная мотивация на успех» для A1 при значении больше 14 или 
«очень высокий уровень нейротизма» для A3 при значении больше 19) и персонализированными рекомендациями 
в формате T/R/C. Преподаватель может просматривать как сводные графики по всей группе, так 
и индивидуальные профили конкретных студентов, что позволяет корректировать педагогическую 
стратегию с учётом психологических особенностей обучающихся.
Дополнительно система предоставляет панель аналитики поведенческих метрик, где агрегируются 
данные трекинга активности мыши по всем студентам с завершённым трекингом. 

На завершающем этапе преподаватель через панель управления получает доступ к агрегированной 
аналитике, графикам психологического профиля и метрикам вовлечённости. Система прогнозирует 
успеваемость и формирует рекомендации по построению дальнейшей образовательной траектории. 
Взаимодействие модулей согласно описанным этапам наглядно показано на 
рисунке~\ref{fig:modules_interaction}.

\begin{figure}[p]
\centering
\resizebox{\textwidth}{!}{%
    \includegraphics{pics/rel}%
}
\caption{Схема взаимодействия модулей системы}
\label{fig:modules_interaction}
\end{figure}

\subsection{Модуль построения профиля обучающегося}

Модуль построения профиля обучающегося является фундаментальным компонентом системы, 
отвечающим за формирование многомерной модели студента. В отличие от существующих 
решений (Moodle, Canvas, Stepik), где модель обучающегося ограничивается журналом 
оценок и историей действий, разрабатываемый модуль интегрирует психологические 
характеристики, поведенческие метрики и контекстные данные.

Компонент психологического тестирования обрабатывает результаты, охватывающие 
четыре ключевых фактора когнитивного профиля:
\begin{itemize}
    \item A1 (Мотивация по шкале МУН) --- уровень учебной мотивации, шкала от 0 до 20 баллов. Определяет направленность обучающегося на успех или избегание неудач;
    \item A2 (Экстраверсия по тесту EPI) --- уровень экстраверсии/интроверсии, шкала от 0 до 24 баллов. Характеризует предпочтительный тип взаимодействия с учебной средой;
    \item A3 (Нейротизм по тесту EPI) --- уровень эмоциональной устойчивости, шкала от 0 до 24 баллов. Критически важен для дозирования стрессовых нагрузок и настройки мотивационной поддержки;
    \item A4 (Темперамент по кругу EPI) --- тип высшей нервной деятельности (1 --- флегматик, 2 --- сангвиник, 3 --- меланхолик, 4 --- холерик).
\end{itemize}

Алгоритм обработки результатов включает строгую валидацию и нормализацию. 
Сырые баллы группируются по базовым факторам (с отбрасыванием суффиксов T/R/C), 
после чего для каждого фактора выбирается наиболее свежий результат. Значения 
жёстко ограничиваются диапазонами шкал.

На основе нормализованных значений алгоритм обращается к таблице интерпретаций 
и генерирует рекомендации в формате JSON для трёх категорий: T (Teacher --- 
тактические рекомендации для преподавателя), R (Reflection --- обратная связь 
для самоанализа студента) и C (Community --- рекомендации по интеграции в учебное комьюнити).

В отличие от большинства рассмотренных платформ (Moodle, Canvas, Stepik), 
ограничивающихся базовыми логами взаимодействия, разрабатываемый модуль 
реализует непрерывный сбор поведенческих данных через специализированный 
API-эндпоинт. API принимает массив координат курсора и временных меток. 
Специализированный алгоритм анализирует скорость движения, резкость изменений 
траектории и паузы, вычисляя:
\begin{itemize}
    \item поведенческую оценку мотивации --- на основе тестов;
    \item поведенческую оценку невротизма --- на основе анализа движений курсора (высокая скорость, резкие изменения направления коррелируют с тревожностью).
\end{itemize}

Эти значения сохраняются в профиле обучающегося, формируя истинно многомерную модель, опирающуюся как на самоотчёт (тесты), так и на объективное поведение.

В отличие от Knewton и ALEKS, где модель обучающегося ориентирована исключительно на предметные знания, разрабатываемый модуль создаёт многомерный профиль, включающий:
\begin{itemize}
    \item психологические характеристики (факторы A1--A4);
    \item поведенческие метрики (трекинг мыши);
    \item контекстные данные (погода, геолокация);
    \item историю прогресса и результатов тестирования.
\end{itemize}

Это позволяет адаптировать не только содержание, но и стиль подачи материала, уровень поддержки, тип мотивационных стимулов.

\subsection{Модуль контекстной адаптации и оценка внешних факторов}
\label{sec:contextual_adaptation}

Контекстная адаптация представляет собой корректировку поведения интеллектуальной обучающей системы (ИОС) на основе информации о внешних условиях функционирования, не зависящих непосредственно от учебного контента. В разрабатываемой системе реализован модуль контекстной адаптации, оценивающий три ключевых параметра: географическое положение, погодные условия и температуру воздуха.

Научное обоснование связи погодных условий с когнитивной продуктивностью восходит к исследованиям в области экологической психологии. Эмпирические данные свидетельствуют, что при температуре ниже 13~°C или выше 33~°C наблюдается статистически значимое снижение точности выполнения когнитивных задач и скорости обработки информации. Кроме того, пасмурная и дождливая погода снижает уровень серотонина, что отрицательно сказывается на концентрации внимания и рабочей памяти --- механизм, хорошо изученный в контексте сезонных аффективных расстройств.

\subsubsection*{Интегральный параметр активности}

Для количественной оценки влияния внешних факторов в системе введён интегральный параметр активности пользователя $P$, рассчитываемый по формуле:
\begin{equation}
    \label{eq:P}
    P = K_{\text{region}} \cdot K_{\text{temp}} \cdot K_{\text{weather}}
\end{equation}
где $K_{\text{region}}$ --- региональный коэффициент, учитывающий климатические особенности; $K_{\text{weather}}$ --- погодный коэффициент, нормализованный к шкале 0--1; $K_{\text{temp}}$ --- температурный коэффициент, отражающий отклонение от комфортного диапазона.

Выбор мультипликативной формы обусловлен тем, что каждый из факторов 
действует как ограничивающее условие: при неблагоприятном значении 
любого из них общая предрасположенность к обучению должна снижаться 
пропорционально. Данный подход согласуется с принципом «слабого 
звена» в эргономических моделях работоспособности.

Значение $K_{\text{temp}}$ вычисляется на основе текущего отклонения температуры с учётом нижней границы устойчивости системы:
\begin{equation}
    \label{eq:Ktemp}
    K_{\text{temp}} = \max\left(0{,}1;\ 1 - \frac{|T_{\text{current}} - T_{\text{comfort}}|}{T_{\text{max\,diff}}}\right)
\end{equation}
где:
\begin{itemize}
    \item $T_{\text{comfort}}$ --- комфортная температура (в системе принята за 20~°C);
    \item $T_{\text{current}}$ --- текущая температура воздуха;
    \item $T_{\text{max\,diff}}$ --- максимальное допустимое отклонение температуры (13~°C), при котором коэффициент снижается до минимального порогового значения.
\end{itemize}

Выбор диапазона 15--25~°C как комфортного опирается на исследования терморегуляции и умственной работоспособности, а значение $T_{\text{comfort}} = 20$~°C соответствует середине этого диапазона и является стандартным нормативом для учебных помещений согласно ГОСТ~30494-2011. Нижняя граница $K_{\text{temp}} = 0{,}1$ введена намеренно: полное обнуление параметра $P$ привело бы к отказу рекомендательной системы, что нецелесообразно, поскольку даже в экстремальных условиях ИОС должна функционировать в минимальном режиме.

Значения коэффициентов определены на основе анализа психофизиологических исследований. Региональные коэффициенты (таблица~\ref{tab:region}) учитывают адаптацию населения к специфическим климатическим зонам.

\begin{table}[htbp]
\centering
\caption{Региональные коэффициенты $K_{\text{region}}$ для регионов России}
\label{tab:region}
\begin{tabular}{|p{6cm}|c|p{6cm}|}
\hline
\textbf{Регион России} & \textbf{$K_{\text{region}}$} & \textbf{Обоснование} \\
\hline
Северные регионы (Мурманская обл., Республика Саха, Чукотка) & 1{,}3 & Экстремальные климатические условия, полярная ночь, низкие температуры \\
\hline
Северо-Западный регион (Санкт-Петербург, Карелия) & 1{,}2 & Высокая влажность, дефицит солнечных дней\\
\hline
Сибирь (Новосибирская, Кемеровская обл.) & 1{,}1 & Резко континентальный климат, большие перепады температур\\
\hline
Дальний Восток (Приморский, Амурская обл.) & 1{,}1 & Муссонный климат, высокая влажность\\
\hline
Центральная Россия (Москва, Владимирская, Тульская обл.) & 1{,}0 & Умеренно-континентальный климат\\
\hline
Южные регионы (Краснодарский край, Ростовская обл., Ставрополье) & 0{,}9 & Мягкий климат\\
\hline
\end{tabular}
\end{table}

Погодный коэффициент $K_{\text{weather}}$ (таблица~\ref{tab:weather}) нормализован 
по текущим метеорологическим условиям. Ясная погода ($K = 1{,}0$) принята за базовый 
уровень оптимальной концентрации. Облачность ($K = 0{,}9$) незначительно снижает 
естественную освещённость, влияя на циркадные ритмы. Дождь ($K = 0{,}7$) создаёт 
акустические помехи и ассоциируется со снижением уровня серотонина. Гроза ($K = 0{,}5$) 
формирует выраженный стрессовый фон. Снег ($K = 0{,}6$) помимо психологического дискомфорта 
создаёт практические препятствия, увеличивая когнитивную нагрузку, связанную с логистикой.

\begin{table}[htbp]
\centering
\caption{Погодные коэффициенты $K_{\text{weather}}$}
\label{tab:weather}
\begin{tabular}{|p{8cm}|c|}
\hline
\textbf{Погодные условия} & \textbf{$K_{\text{weather}}$} \\
\hline
Ясно & 1{,}0 \\
\hline
Облачно & 0{,}9 \\
\hline
Дождь & 0{,}7 \\
\hline
Снег & 0{,}6 \\
\hline
Гроза & 0{,}5 \\
\hline
\end{tabular}
\end{table}

Архитектура модуля контекстной адаптации обеспечивает надёжный сбор и обработку данных. Модуль реализует трёхуровневую систему отказоустойчивости: при недоступности внешних API используются кэшированные значения, а при полном отсутствии данных применяются дефолтные региональные коэффициенты. Взаимодействие компонентов визуализировано на рисунке~\ref{fig:context_pipeline}.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\linewidth]{pics/Picture3}
\caption{Конвейер обработки данных модуля контекстной адаптации}
\label{fig:context_pipeline}
\end{figure}

Процесс обработки данных включает следующие последовательные этапы:
\begin{enumerate}
    \item Идентификация местоположения. IP-адрес пользователя извлекается из HTTP-запроса (с учётом заголовков прокси-серверов) и передаётся в RESTful API сервиса геолокации. Сервис возвращает JSON-объект, содержащий информацию о стране, регионе, городе и географических координатах.
    \item Получение метеорологических данных. На основе полученных координат формируется GET-запрос к API погодного сервиса, предоставляющему актуальные данные о температуре, атмосферном давлении, влажности, а также код текущего погодного условия.
    \item Обработка ошибок и резервные механизмы. Модуль реализует трёхуровневую систему отказоустойчивости: (а) повторные запросы при временной недоступности API (retry logic с экспоненциальной задержкой); (б) использование кэшированных значений при превышении таймаута; (в) применение дефолтных региональных коэффициентов при полной недоступности внешних сервисов.
    \item Расчёт интегрального показателя. На основе нормализованных значений $K_{\text{region}}$, $K_{\text{temp}}$ и $K_{\text{weather}}$ вычисляется параметр $P$ по формуле~\eqref{eq:P}, отражающий текущую предрасположенность обучающегося к эффективной учебной деятельности.
    \item Интеграция с рекомендательной системой. Рассчитанное значение $P$ передаётся в функцию формирования рекомендаций, где используется как весовой коэффициент. При низком значении $P$ (неблагоприятные внешние условия) алгоритм может смещать приоритеты в сторону курсов с меньшей когнитивной нагрузкой или корректировать ожидаемое время выполнения заданий, обеспечивая педагогически обоснованную адаптацию.
\end{enumerate}

\subsection{Модуль предоставления знаний и конструктор учебных курсов}
\label{sec:knowledge_module}

Модуль предоставления знаний является центральным компонентом
разрабатываемой системы и непосредственным объектом настоящего
исследования. Именно здесь реализуется ключевая идея работы:
преподаватель создаёт курс через визуальный интерфейс, задавая
структуру и метаданные, а система на основе когнитивного профиля
студента автоматически формирует персонализированную учебную
траекторию. Модуль включает три тесно связанных компонента:
визуальный конструктор курсов, механизм адаптации подачи контента
под профиль обучающегося и автоматическую обработку учебных материалов.

\subsubsection{Архитектура конструктора курсов}

Конструктор реализован на базе Django MVT через набор классовых
представлений: \texttt{CourseCreateView} и \texttt{CourseUpdateView}
управляют жизненным циклом курса, \texttt{TopicCreateView} и
\texttt{TopicUpdateView} — темами, \texttt{TopicContentCreateView} —
учебными материалами внутри темы. Доступ ко всем представлениям
защищён миксином \texttt{UserPassesTestMixin}, разграничивающим
права преподавателей и студентов на уровне Django-запроса.

Модель данных строится вокруг трёх сущностей. \texttt{Course}
хранит метаданные курса: название, описание, теги, уровень
сложности, целевую учебную группу и статус публикации.
\texttt{Topic} представляет учебную тему и содержит, помимо
контентных полей, два отношения «многие ко многим» к самой себе:
\texttt{prerequisites} (предварительные требования) и
\texttt{related\_topics} (связанные темы). Именно эти отношения
образуют направленный граф зависимостей, на основе которого
строится учебная траектория. \texttt{TopicContent} хранит
конкретный учебный объект — текст, видео, аудио или файл —
и дополнительно содержит поля \texttt{difficulty\_level} и
\texttt{recommended\_profile}, позволяющие адаптационному
движку выбирать подходящий формат для конкретного студента.

Граф зависимостей между темами визуализируется в интерфейсе
преподавателя с помощью библиотеки vis-network: узлы
соответствуют темам, направленные рёбра — отношениям
предварительных требований. Редактирование графа происходит
интерактивно; изменения сохраняются через API-эндпоинт
\texttt{/api/topics/\{id\}/connections/}, принимающий
JSON-описание связей и обновляющий отношения в БД за одну
транзакцию. Такой подход позволяет преподавателю выстраивать
сложные учебные маршруты без написания кода, что принципиально
отличает систему от ALEKS и Knewton, где структура курса
задаётся контент-провайдером.

Схема модели данных конструктора курсов представлена на
рисунке~\ref{fig:course_model}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  entity/.style={rectangle, draw, rounded corners=3pt,
                 minimum width=5.5cm, minimum height=1.0cm,
                 font=\small, align=center, fill=blue!8},
  field/.style={rectangle, draw=gray!60, minimum width=5.5cm,
                minimum height=0.75cm, font=\footnotesize,
                align=center, fill=white, text width=5.3cm,
                inner sep=3pt},
  arr/.style={->, >=stealth, thick},
  darr/.style={<->, >=stealth, thick, dashed}
]

%% Course
\node[entity] (course) at (0, 8.0) {\textbf{Course}};
\node[field, below=0pt of course] (c1) {id, title, description};
\node[field, below=0pt of c1]     (c2) {tags, difficulty\_level};
\node[field, below=0pt of c2]     (c3) {group (FK), is\_published};

%% Topic
\node[entity] (topic) at (0, 5.0) {\textbf{Topic}};
\node[field, below=0pt of topic] (t1) {id, course (FK), title};
\node[field, below=0pt of t1]    (t2) {prerequisites (M2M~→~self)};
\node[field, below=0pt of t2]    (t3) {related\_topics (M2M~→~self)};
\node[field, below=0pt of t3]    (t4) {order, is\_published};

%% TopicContent
\node[entity] (content) at (0, 2.0) {\textbf{TopicContent}};
\node[field, below=0pt of content] (tc1) {id, topic (FK), content\_type};
\node[field, below=0pt of tc1]     (tc2) {difficulty\_level};
\node[field, below=0pt of tc2]     (tc3) {recommended\_profile};

%% CognitiveProfile (внешний, дальше вправо)
\node[entity, fill=orange!10] (profile) at (8.5, 3.5) {\textbf{CognitiveProfile}};
\node[field, fill=orange!5, below=0pt of profile] (p1) {A1 (мотивация)};
\node[field, fill=orange!5, below=0pt of p1]      (p2) {A2 (экстраверсия)};
\node[field, fill=orange!5, below=0pt of p2]      (p3) {A3 (нейротизм)};

%% Связи
\draw[arr] (c3.south) -- (t1.north)
      node[midway, right, font=\footnotesize] {1 : N};
\draw[arr] (t4.south) -- (content.north)
      node[midway, right, font=\footnotesize] {1 : N};
\draw[darr] (t2.east) to[out=0, in=90] (t3.east);

%% Стрелка адаптации — обходная, не пересекает TopicContent
\draw[arr, dashed, orange!70] (profile.west) -- ++(-1.5,0) |- (tc3.east)
      node[midway, above right, font=\footnotesize, orange!80] {адаптация};

\end{tikzpicture}
\caption{Модель данных конструктора курсов и её связь с когнитивным профилем}
\label{fig:course_model}
\end{figure}

\subsubsection{Алгоритм формирования учебной траектории}

Персонализация в конструкторе курсов реализована на двух уровнях:
упорядочивание тем и ранжирование контента внутри темы. Оба механизма
опираются на когнитивный профиль студента, хранящийся в модели
психпро с полями \texttt{factor\_a1}--\texttt{factor\_a4}.

\textbf{Адаптивная сортировка тем.}
При загрузке страницы курса вызывается функция
\texttt{get\_personalized\_topics(course, profile)} из модуля
\texttt{learnsys/utils/psych\_adapter.py}. Функция извлекает все видимые
темы с предзагрузкой отношений \texttt{prerequisites} и для каждой темы
вычисляет корректирующий балл~$adj$ на основе поля
\texttt{psych\_characteristics} — JSON-словаря с пятью параметрами:
\texttt{pace}, \texttt{structure}, \texttt{interaction},
\texttt{stress\_level}, \texttt{challenge}. Правила коррекции
соответствуют четырём факторам профиля:

\begin{itemize}
  \item \textbf{A1 (мотивация):} при низкой мотивации ($\leq 9$) понижается
  приоритет тем с быстрым темпом (\texttt{pace=fast}) и повышается приоритет
  тем с высокой структурой (\texttt{structure=high}) и низкой сложностью;
  при высокой мотивации ($\geq 14$) приоритет отдаётся темам с
  \texttt{challenge=high} и \texttt{pace=fast}.
  \item \textbf{A2 (экстраверсия):} интроверты получают бонус к темам с
  \texttt{interaction=independent}, экстраверты — к темам с
  \texttt{interaction=group} и быстрым темпом.
  \item \textbf{A3 (нейротизм):} при высоком нейротизме ($\geq 14$)
  понижается приоритет стрессовых тем и повышается приоритет тем с
  высокой структурой и медленным темпом.
  \item \textbf{A4 (темперамент):} флегматик получает бонус к темам с
  медленным темпом, холерик — с быстрым.
\end{itemize}

Итоговый балл каждой темы вычисляется как
$\text{final\_score} = \text{order\_index} - adj$. Темы сортируются
по двум ключам: сначала по глубине цепочки \texttt{prerequisites}
(рекурсивно вычисляемой функцией \texttt{\_get\_max\_prerequisite\_depth}),
что гарантирует соблюдение учебных зависимостей, затем по
\texttt{final\_score} внутри одного уровня глубины. Таким образом,
обязательная последовательность тем сохраняется, а их относительный
порядок внутри допустимых веток адаптируется под профиль студента.

\textbf{Адаптивный выбор формата контента.}
Представление \texttt{TopicDetailView} при открытии темы извлекает
все видимые объекты \texttt{TopicContent}, упорядоченные по
\texttt{order\_index}. Затем вычисляются веса форматов на основе двух
значений из профиля пользователя: \texttt{activity\_score} (косвенная
мера мотивации) и атрибута \texttt{neuroticism} (если задан). Весовая
функция для каждого типа контента:

\begin{itemize}
  \item \texttt{video}: $0{,}6 \cdot \text{motivation} + 0{,}4 \cdot (10 - \text{neuroticism})$;
  \item \texttt{text}: $0{,}7 \cdot \text{neuroticism} + 0{,}3 \cdot (10 - \text{motivation})$;
  \item \texttt{quiz}: $0{,}5 \cdot \text{motivation} + 0{,}3 \cdot |\text{neuroticism} - 5|$.
\end{itemize}

Для остальных типов (\texttt{file}, \texttt{image}, \texttt{audio})
вес фиксирован на уровне $5{,}0$. Все веса нормализуются делением
на максимальное значение, после чего из доступных в теме типов
выбирается тот, чей нормализованный балл максимален. Соответствующий
объект \texttt{TopicContent} передаётся в шаблон как
\texttt{preferred\_content} и отображается студенту первым, тогда как
остальные материалы остаются доступны для самостоятельного выбора.

Например, студент с высоким нейротизмом (\texttt{A3} $> 16$) получит
приоритет текстового формата перед видео с таймером — это снижает
когнитивную нагрузку в ситуации повышенной тревожности. Студент с
высокой мотивацией и низким нейротизмом получит видео или тест в
качестве приоритетного формата.

\subsubsection{Автоматический конструктор тестов}

Загрузка учебных материалов запускает автоматический конвейер
генерации тестовых заданий, представленный на
рисунке~\ref{fig:course_builder_flow}. Преподаватель загружает
файл (PDF, DOCX, видео или аудио); система извлекает текст
(для мультимедиа через транскрипцию ffmpeg + Whisper) и
передаёт его в генератор вопросов на базе языковой модели.
Полученные вопросы проходят семантическую дедупликацию:
эмбеддинги вычисляются через Sentence-BERT, кластеризация
выполняется алгоритмом DBSCAN, и из каждого кластера
оставляется только один репрезентативный вопрос. Каждому
прошедшему отбор вопросу автоматически присваиваются
начальные параметры модели Раша: дискриминативность,
вероятность случайного угадывания и параметр сложности
в зависимости от уровня темы (базовый / средний / повышенный).

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=1.4cm and 2.8cm,
    on grid,
    startstop/.style={rectangle, rounded corners, minimum width=3.8cm,
                     minimum height=0.9cm, align=center, draw=black,
                     fill=white, font=\small},
    process/.style={rectangle, minimum width=3.8cm, minimum height=0.9cm,
                   align=center, draw=black, fill=white, font=\small},
    arrow/.style={thick,->,>=stealth}
]
\node (start)  [startstop] {Загрузка материала\\преподавателем};
\node (proc1)  [process, below=of start]  {Извлечение текста\\(PDF / ffmpeg+Whisper)};
\node (proc2)  [process, below=of proc1]  {Генерация вопросов\\(LLM)};
\node (proc3)  [process, below=of proc2]  {Семантич. дедупликация\\(Sentence-BERT + DBSCAN)};
\node (proc4)  [process, below=of proc3]  {Присвоение IRT-параметров\\(модель Раша)};
\node (stop)   [startstop, below=of proc4]{Банк заданий готов\\к адаптивному тестированию};
\draw [arrow] (start) -- (proc1);
\draw [arrow] (proc1) -- (proc2);
\draw [arrow] (proc2) -- (proc3);
\draw [arrow] (proc3) -- (proc4);
\draw [arrow] (proc4) -- (stop);
\end{tikzpicture}
\caption{Конвейер автоматической генерации тестовых заданий}
\label{fig:course_builder_flow}
\end{figure}

Результатом работы конвейера является размеченный банк
заданий, непосредственно используемый модулем адаптивного
тестирования (раздел~\ref{sec:knowledge_identification}).
Таким образом, конструктор курсов замыкает цикл: преподаватель
загружает материал один раз, а система самостоятельно
строит из него и адаптированный учебный контент, и
диагностический инструментарий.